; XEvolMaxExporter.def : Declares the module parameters for the DLL.

LIBRARY      "XEvolMaxExporter.dlu"

EXPORTS
   LibDescription       @1       PRIVATE
   LibNumberClasses     @2       PRIVATE
   LibClassDesc         @3       PRIVATE
   LibVersion           @4       PRIVATE
SECTIONS
   .data READ WRITE
